import contentTypesConstants from '@/constants/contentTypes.constants';

const allowedContentTypes = [contentTypesConstants.JSON];

const eventCategoryConstants = {
    allowedContentTypes,
};

export default eventCategoryConstants;
